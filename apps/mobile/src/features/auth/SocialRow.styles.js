import { StyleSheet } from "react-native";

export default StyleSheet.create({
  wrap: { alignItems: "center", marginTop: 30 },
  orText: { color: "#64748B", fontSize: 16, marginBottom: 20 },
  row: { flexDirection: "row", gap: 20 },
  circle: {
    width: 62,
    height: 62,
    borderRadius: 31,
    alignItems: "center",
    justifyContent: "center",
  },
});
